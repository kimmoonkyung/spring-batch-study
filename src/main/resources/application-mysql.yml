spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://127.0.0.1:3306/batch_study?characterEncoding=UTF-8&serverTimezone=UTC&rewriteBatchedStatements=true
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: Qpalzm!2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
batch:
  initialize-schema: never